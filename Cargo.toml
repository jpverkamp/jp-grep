[package]
name = "codecrafters-grep"
version = "0.1.0"
authors = ["JP Verkamp <me@jverkamp.com>"]
edition = "2021"
rust-version = "1.80"

[dependencies]
clap = { version = "4.5.16", features = ["derive"] }
